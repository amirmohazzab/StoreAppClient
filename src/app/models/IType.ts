export interface IType {
    created?: Date,
    createdBy?: string,
    description?: string,
    id: number,
    isActive?: boolean,
    isDelete?: boolean,
    lastModified?: Date,
    lastModifiedBy?: string,
    summary?: string,
    title?: string,
}