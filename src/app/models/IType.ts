export interface IType {
    id: number,
    created?: Date,
    createdBy?: string,
    description?: string,
    isActive?: boolean,
    isDelete?: boolean,
    lastModified?: Date,
    lastModifiedBy?: string,
    summary?: string,
    title?: string,
}